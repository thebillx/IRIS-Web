# Web UI

The primary interface is a React application built with Vite and opened in a standard browser. React is justified by the expected stateful operational UI; Vite keeps the development and production build small.

During bootstrap, the UI displays the product name, local-runtime label, and health-derived connection status. It does not advertise unavailable platform capabilities.
