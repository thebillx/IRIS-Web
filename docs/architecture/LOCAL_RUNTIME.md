# Local runtime

The daemon is a plain Node.js process that listens on loopback and supplies local HTTP, WebSocket, or server-sent-event contracts as they become necessary. The bootstrap exposes only `GET /health`.

Runtime identity, process lifecycle, single-writer authority, and native capability execution belong to later missions. No placeholder may imply that those guarantees already exist.
