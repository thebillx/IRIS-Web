# Product intent

IRIS is a macOS-first local tool. A browser is the primary interface; a local daemon owns runtime behavior and reaches filesystem, process, and native capabilities only under explicit local authority.

The runtime does not require a cloud control plane. GitHub is used for source history, not product operation. Remote access, cloud hosting, Windows support, and an Electron shell are outside the current target.
